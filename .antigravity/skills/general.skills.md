# General Skills (MacroMogul)

- Always ask: "Is this user-visible or internal?"
- If user-visible naming/labels are requested, enforce them across:
  - i18n translation files
  - index.html title/meta
  - README/docs
- Prefer grep-driven verification before commit.
- Avoid sweeping refactors unless required.
